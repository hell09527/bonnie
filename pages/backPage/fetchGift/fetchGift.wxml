<scroll-view>

<view class="nav">
<view class="headPortrait">
<image mode="widthFix"   src="{{goodsInfo.user_headimg}}"></image>
<view class="headName">{{goodsInfo.member_name}}
</view>
</view>
</view>
<view class="body">
<view class="body_title" style='padding:0 20px;'>送你一份黑科技礼物</view>
<view class="cards">
<image src="{{imgUrls}}" mode="widthFix"></image>
</view>
<view class="zhufu">祝福留言：{{goodsInfo.buyer_message}}
</view>
</view>
<block wx:if="{{order_status==11}}">
<view wx:if='{{unregistered==1 || tel==""}}'>
<view class="footer">
<button class="get" catchtap='Crossroad' >领取礼物</button>
</view>
</view>

<view wx:if='{{unregistered==0 && tel!="" }}'>
<view class="footer">
<button class="get"  catchtap='triumph'>领取礼物</button>
</view>
</view>
</block>
<block wx:if="{{order_status==1}}">
<view class="footer">
<button class="get"  disabled='true'>礼物已领取</button>
</view>
</block>
</scroll-view>

<view class="ck-mask" bindtap="hideModal" catchtouchmove="preventTouchMove" wx:if="{{showModal}}"></view>
    <!-- 登录弹出框 -->
    <view class="ck-dialog" wx:if="{{showModal}}">
 
  <view class="ck-title">手机号授权绑定成功
  </view>
  <view class="ck-footer">
   <button  class="btn-confirm"  data-status="confirm" open-type="getUserInfo" bindgetuserinfo="bindgetuserinfo">确   定</button>
  </view> 
</view>
<!-- //客服 -->
           <!-- <button class="a new-btn buy-cart" open-type="contact" session-from="weapp" class="yuan" style='right:140rpx;'>
                <view><image src="https://static.bonnieclyde.cn/tt.png"  mode='widthFix' ></image></view>
            </button> -->
<view class="yuan1"  catchtap='Backindex'>去送礼</view>
   <!-- 选择登录方式  -->
   <view class="login-modal-mask" bindtap="hideModal" catchtouchmove="preventTouchMove" wx:if="{{Choice}}"></view>
  <view class="login-modal-dialog" wx:if="{{Choice}}">
    <view class="gift_title">请登录...</view>
    <view class="gift-text">请你先登录，才可以同步个人数据！</view>
     <button  class="btn-common"  open-type="getPhoneNumber" bindgetphonenumber="getPhoneNumber" >微信登录</button>
    <button  class="btn-common"  data-status="mobile"  catchtap='Branch'>手机号登录</button>
   <button  class="btn-common"  data-status="no" catchtap='Branch' style='background:transparent;font-size:14px;'>暂不登录</button>
   </view>
   <!-- 选择登录方式   end-->